local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Lighting = game:GetService("Lighting")
local TweenService = game:GetService("TweenService")
local Debris = game:GetService("Debris")

local player = Players.LocalPlayer
local playerGui = player:FindFirstChild("PlayerGui")
local mainUI = playerGui and playerGui:FindFirstChild("MainUI")
local initiator = mainUI and mainUI:FindFirstChild("Initiator")

if initiator then
    local mainGame = initiator:FindFirstChild("Main_Game")
    if mainGame and mainGame:IsA("ModuleScript") then
        local mainGameModule = require(mainGame)
        if type(mainGameModule.caption) == "function" then
            mainGameModule.caption("Hardcore Mode Recreation By ItzzGabbbb", true)
            wait(4)
            mainGameModule.caption("Credits to Noonie & Ping for the original hardcore mode", true)
            wait(3)
            mainGameModule.caption("*tickles your balls* :3", true)
        end
    end
end


-- A-1 Entity
coroutine.wrap(function()
    while true do
        wait(math.random(175,235))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
            ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A-1",
		Asset = "rbxassetid://16639419547",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = false,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 20,
		Delay = 2,
		Reversed = true
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		IgnoreHiding = false,
		Range = 100,
		Amount = 1
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"You die to A-25", "He is rebounding", "If u here a sound like alert", "is him spawn"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
    else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
        end)
    end
end)()

-- A-25 Entity
coroutine.wrap(function()
    while true do
        wait(math.random(250, 300))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
            ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A-25",
		Asset = "rbxassetid://16941610885",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 65,
		Delay = 2,
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 0
	},
	Damage = {
		Enabled = true,
		IgnoreHiding = false,
		Range = 55,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"You die to A-25", "He is rebounding", "If u here a sound like alert", "is him spawn"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
	local sound = game.Workspace["A-25"].RushNew.Spawn:Play()
	local sound = game.Workspace["A-25"].RushNew.PlaySound:Play()
	local sound = game.Workspace["A-25"].RushNew.PlaySound2:Play()
	local sound = game.Workspace["A-25"].RushNew.PlaySound3:Play()
	local entity = game.Workspace["A-25"]
	local part = game.Workspace["A-25"].RushNew
	-- variables

	local rn = Random.new()

	while entity:GetAttribute("Enabled") == true do
		local posintensity = 0.8
		local rotateintensity = 13
		local delaytime = entity:GetAttribute("Delay")
		task.wait(delaytime)
		entity.RushNew:WaitForChild("Entity").StudsOffset = Vector3.new(rn:NextNumber(-posintensity, posintensity), rn:NextNumber(-posintensity, posintensity), 0)
		entity.RushNew:WaitForChild("Entity").Image1.Rotation = rn:NextNumber(-rotateintensity, rotateintensity)
	end
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local jumpscareSound = Instance.new("Sound")
		jumpscareSound.Name = "Jumpscare"
		jumpscareSound.SoundId = "rbxassetid://6459610652"
		jumpscareSound.Volume = 10
		jumpscareSound.PlaybackSpeed = 1.4
		jumpscareSound.Parent = workspace
		jumpscareSound:Play()

		-- Tự động dọn dẹp Sound sau 5 giây để tránh rác dung lượng
		game:GetService("Debris"):AddItem(jumpscareSound, 5)

		local dis = Instance.new("DistortionSoundEffect")
		dis.Level = 1
		dis.Enabled = true
		dis.Parent = jumpscareSound

		local pitch = Instance.new("PitchShiftSoundEffect")
		pitch.Octave = 0.62
		pitch.Enabled = true
		pitch.Parent = jumpscareSound

		local chorus = Instance.new("ChorusSoundEffect")
		chorus.Depth = 0.15
		chorus.Mix = 0.5
		chorus.Rate = 0.5
		chorus.Enabled = true
		chorus.Parent = jumpscareSound

		local echo = Instance.new("EchoSoundEffect")
		echo.Delay = 0.02
		echo.Feedback = 1
		echo.Enabled = true
		echo.Parent = jumpscareSound

		local userInterface = game:GetService("UserInputService")
		local Players = game:GetService("Players")
		local TweenService = game:GetService("TweenService")

		local player = Players.LocalPlayer
		local playerGui = player:WaitForChild("PlayerGui")
		local rng = Random.new()

		-- Tạo ScreenGui
		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "JumpscareGui"
		screenGui.ResetOnSpawn = false
		screenGui.IgnoreGuiInset = true
		screenGui.DisplayOrder = 999999
		screenGui.Parent = playerGui

		-- Tạo background
		local background = Instance.new("Frame")
		background.Name = "Background"
		background.Size = UDim2.fromScale(1, 1)
		background.Position = UDim2.fromScale(0, 0)
		background.BackgroundColor3 = Color3.new(0, 0, 0)
		background.BorderSizePixel = 0
		background.Parent = screenGui

		-- Tạo ImageLabel
		local imageLabel = Instance.new("ImageLabel")
		imageLabel.Name = "ImageLabel"
		imageLabel.BackgroundTransparency = 1
		imageLabel.AnchorPoint = Vector2.new(0.5, 0.5)
		imageLabel.Position = UDim2.fromScale(0.5, 0.5)
		imageLabel.Size = UDim2.new(0, 0.001, 0, 0.001)
		imageLabel.Image = "rbxassetid://15683707568"
		imageLabel.ZIndex = 2
		imageLabel.Parent = screenGui -- ĐÃ SỬA: Gán Parent cho ImageLabel xuất hiện

		local fullTween = TweenService:Create(
			imageLabel,
			TweenInfo.new(0.7, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{
				Size = UDim2.new(0, 1250, 0, 1250)
			}
		)

		fullTween:Play()

		-- ĐÃ SỬA: Kiểm tra xem thiết bị có phải là Mobile/Cảm ứng không
		if userInterface.TouchEnabled and not userInterface.KeyboardEnabled then
			-- Chế độ dành cho Điện thoại / Máy tính bảng
			for i = 1, 60 do
				local mode = rng:NextInteger(1, 3)

				if mode == 1 then
					background.BackgroundColor3 = Color3.new(0, 1, 0)
					imageLabel.ImageColor3 = Color3.new(0, 1, 0)
					imageLabel.Position = UDim2.new(rng:NextNumber(0.2, 0.8), 0, rng:NextNumber(0.2, 0.8), 0)
				elseif mode == 2 then
					background.BackgroundColor3 = Color3.new(0, 0.760784, 0)
					imageLabel.ImageColor3 = Color3.new(0, 0, 0)
					imageLabel.Position = UDim2.new(rng:NextNumber(0.2, 0.8), 0, rng:NextNumber(0.2, 0.8), 0)
				else
					background.BackgroundColor3 = Color3.new(0, 0, 0)
					imageLabel.ImageColor3 = Color3.new(0, 0.760784, 0)
					imageLabel.Position = UDim2.new(rng:NextNumber(0.2, 0.8), 0, rng:NextNumber(0.2, 0.8), 0)
				end

				imageLabel.Rotation = rng:NextInteger(-10, 10)
				task.wait()
			end
		else
			-- Chế độ dành cho Máy tính (PC/Laptop)
			for i = 1, 140 do
				local mode = rng:NextInteger(1, 3)

				if mode == 1 then
					background.BackgroundColor3 = Color3.new(0, 1, 0)
					imageLabel.ImageColor3 = Color3.new(0, 1, 0)
					imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
				elseif mode == 2 then
					background.BackgroundColor3 = Color3.new(0, 0.760784, 0)
					imageLabel.ImageColor3 = Color3.new(0, 0, 0)
					imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
				else
					background.BackgroundColor3 = Color3.new(0, 0, 0)
					imageLabel.ImageColor3 = Color3.new(0, 0.760784, 0)
					imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
				end

				imageLabel.Rotation = rng:NextInteger(-10, 10)
				task.wait()
			end
		end

		-- Dọn dẹp GUI sau khi thực hiện xong jumpscare
		screenGui:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
        end)
    end
end)()

-- A-45 Entity
coroutine.wrap(function()
    while true do
        wait(math.random(295,375))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
            ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A45",
		Asset = "rbxassetid://16894851383",
		HeightOffset = 1
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 200,
		Values = {70, 88.5, 0.1, 0.1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 126,
		Delay = 3.5,
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
	local A451 = Instance.new("Sound")
	A451.SoundId = "rbxassetid://1843025759"
	A451.Volume = 10
	A451.PlaybackSpeed = 2
	A451.Parent = workspace
	A451:Play()
	
	local dis = Instance.new("DistortionSoundEffect")
	dis.Level = 0.75
	dis.Parent = A451
	
	local root = game.Workspace.A45.RushNew
	local face = root.MonsterGui
	local image = face.Image1
	local pos_x = 0.1
	local pos_y = 0.1
	local pos_z = 0.1
	local rot = 10


	while true do
		wait()
		face.ExtentsOffsetWorldSpace = Vector3.new(Random.new():NextNumber(-pos_x,pos_x),Random.new():NextNumber(-pos_y,pos_y),Random.new():NextNumber(-pos_z,pos_z))
		image.Rotation = math.random(-rot,rot)
	end
	
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")	
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
	local A45 = Instance.new("Sound")
	A45.SoundId = "rbxassetid://6700552345"
	A45.Volume = 10
	A45.Parent = workspace
	A45:Play()
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local Players = game:GetService("Players")
		local TweenService = game:GetService("TweenService")

		local player = Players.LocalPlayer
		local playerGui = player:WaitForChild("PlayerGui")
		local rng = Random.new()

		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "JumpscareGui"
		screenGui.ResetOnSpawn = false
		screenGui.IgnoreGuiInset = true
		screenGui.DisplayOrder = 999999
		screenGui.Parent = playerGui

		-- Tạo background
		local background = Instance.new("Frame")
		background.Name = "Background"
		background.Size = UDim2.fromScale(1, 1)
		background.Position = UDim2.fromScale(0, 0)
		background.BackgroundColor3 = Color3.new(0, 0, 0)
		background.BorderSizePixel = 0
		background.Parent = screenGui

		-- Tạo ImageLabel
		local imageLabel = Instance.new("ImageLabel")
		imageLabel.Name = "ImageLabel"
		imageLabel.BackgroundTransparency = 1
		imageLabel.AnchorPoint = Vector2.new(0.5, 0.5)
		imageLabel.Position = UDim2.fromScale(0.5, 0.5)
		imageLabel.Size = UDim2.new(0, 0.001, 0, 0.001)
		imageLabel.Image = "rbxassetid://13581219246"
		imageLabel.Parent = background

		-- Tạo Sound
		local jumpscareSound = Instance.new("Sound")
		jumpscareSound.Name = "Jumpscare"
		jumpscareSound.SoundId = "rbxassetid://6459610652"
		jumpscareSound.Volume = 10
		jumpscareSound.PlaybackSpeed = 0.8
		jumpscareSound.Parent = imageLabel

		local dis = Instance.new("DistortionSoundEffect")
		dis.Level = 1
		dis.Enabled = true
		dis.Parent = jumpscareSound

		local pitch = Instance.new("PitchShiftSoundEffect")
		pitch.Octave = 0.62
		pitch.Enabled = true
		pitch.Parent = jumpscareSound

		local chorus = Instance.new("ChorusSoundEffect")
		chorus.Depth = 0.15
		chorus.Mix = 0.5
		chorus.Rate = 0.5
		chorus.Enabled = true
		chorus.Parent = jumpscareSound

		local echo = Instance.new("EchoSoundEffect")
		echo.Delay = 0.02
		echo.Feedback = 1
		echo.Enabled = true
		echo.Parent = jumpscareSound

		jumpscareSound:Play()

		local fullTween = TweenService:Create(
			imageLabel,
			TweenInfo.new(0.45, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{
				Size = UDim2.new(0, 1250, 0, 1250)
			}
		)

		fullTween:Play()

		for i = 1, 45 do
			local mode = rng:NextInteger(1, 3)

			if mode == 1 then
				background.BackgroundColor3 = Color3.new(0.772549, 0.513725, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			elseif mode == 2 then
				background.BackgroundColor3 = Color3.new(1, 0.666667, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			else
				background.BackgroundColor3 = Color3.new(0, 0, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			end

			imageLabel.Rotation = rng:NextInteger(-10, 10)
			task.wait(0.00001)
		end


		screenGui:Destroy()
		imageLabel:Destroy()
		background:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)

        end)
    end
end)()

-- A-60 Entity
coroutine.wrap(function()
    while true do
        wait(math.random(345,425))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
           ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A60",
		Asset = "rbxassetid://108413369217199",
		HeightOffset = 1
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 50,
		Values = {70, 88.5, 0.1, 0.1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 80,
		Delay = 4,
		Reversed = false
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
	local A451 = Instance.new("Sound")
	A451.SoundId = "rbxassetid://6351509719"
	A451.Volume = 10
	A451.PlaybackSpeed = 1
	A451.Parent = workspace
	A451:Play()
	
	local dis = Instance.new("DistortionSoundEffect")
	dis.Level = 0.75
	dis.Parent = A451
	
	local root = game.Workspace.A45.RushNew
	local face = root.MonsterGui
	local image = face.Image1
	local pos_x = 0.1
	local pos_y = 0.1
	local pos_z = 0.1
	local rot = 10


	while true do
		wait()
		face.ExtentsOffsetWorldSpace = Vector3.new(Random.new():NextNumber(-pos_x,pos_x),Random.new():NextNumber(-pos_y,pos_y),Random.new():NextNumber(-pos_z,pos_z))
		image.Rotation = math.random(-rot,rot)
	end
	
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
	local A45 = Instance.new("Sound")
	A45.SoundId = "rbxassetid://6700552345"
	A45.Volume = 10
	A45.Parent = workspace
	A45:Play()
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local Players = game:GetService("Players")
		local TweenService = game:GetService("TweenService")

		local player = Players.LocalPlayer
		local playerGui = player:WaitForChild("PlayerGui")
		local rng = Random.new()

		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "JumpscareGui"
		screenGui.ResetOnSpawn = false
		screenGui.IgnoreGuiInset = true
		screenGui.DisplayOrder = 999999
		screenGui.Parent = playerGui

		-- Tạo background
		local background = Instance.new("Frame")
		background.Name = "Background"
		background.Size = UDim2.fromScale(1, 1)
		background.Position = UDim2.fromScale(0, 0)
		background.BackgroundColor3 = Color3.new(0, 0, 0)
		background.BorderSizePixel = 0
		background.Parent = screenGui

		-- Tạo ImageLabel
		local imageLabel = Instance.new("ImageLabel")
		imageLabel.Name = "ImageLabel"
		imageLabel.BackgroundTransparency = 1
		imageLabel.AnchorPoint = Vector2.new(0.5, 0.5)
		imageLabel.Position = UDim2.fromScale(0.5, 0.5)
		imageLabel.Size = UDim2.new(0, 0.001, 0, 0.001)
		imageLabel.Image = "rbxassetid://13581219246"
		imageLabel.Parent = background

		-- Tạo Sound
		local jumpscareSound = Instance.new("Sound")
		jumpscareSound.Name = "Jumpscare"
		jumpscareSound.SoundId = "rbxassetid://6459610652"
		jumpscareSound.Volume = 10
		jumpscareSound.PlaybackSpeed = 0.8
		jumpscareSound.Parent = imageLabel

		local dis = Instance.new("DistortionSoundEffect")
		dis.Level = 1
		dis.Enabled = true
		dis.Parent = jumpscareSound

		local pitch = Instance.new("PitchShiftSoundEffect")
		pitch.Octave = 0.62
		pitch.Enabled = true
		pitch.Parent = jumpscareSound

		local chorus = Instance.new("ChorusSoundEffect")
		chorus.Depth = 0.15
		chorus.Mix = 0.5
		chorus.Rate = 0.5
		chorus.Enabled = true
		chorus.Parent = jumpscareSound

		local echo = Instance.new("EchoSoundEffect")
		echo.Delay = 0.02
		echo.Feedback = 1
		echo.Enabled = true
		echo.Parent = jumpscareSound

		jumpscareSound:Play()

		local fullTween = TweenService:Create(
			imageLabel,
			TweenInfo.new(0.45, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{
				Size = UDim2.new(0, 1250, 0, 1250)
			}
		)

		fullTween:Play()

		for i = 1, 45 do
			local mode = rng:NextInteger(1, 3)

			if mode == 1 then
				background.BackgroundColor3 = Color3.new(0.772549, 0.513725, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			elseif mode == 2 then
				background.BackgroundColor3 = Color3.new(1, 0.666667, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			else
				background.BackgroundColor3 = Color3.new(0, 0, 0)
				imageLabel.ImageColor3 = Color3.new(1, 1, 1)
				imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			end

			imageLabel.Rotation = rng:NextInteger(-10, 10)
			task.wait(0.00001)
		end


		screenGui:Destroy()
		imageLabel:Destroy()
		background:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)

        end)
    end
end)()

-- A-75 Entity
coroutine.wrap(function()
    while true do
        wait(math.random(450,495))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
           ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A75",
		Asset = "rbxassetid://16627784040",
		HeightOffset = 1
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 50,
		Values = {70, 88.5, 0.1, 0.1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 50,
		Delay = 4,
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 0.5
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
    else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)

        end)
    end
end)()

-- A-105 Entity 
coroutine.wrap(function()
    while true do
        wait(math.random(550,595))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()
          ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A-105",
		Asset = "rbxassetid://16403472490",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 500,
		Delay = 0.9,
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 40,
		Amount = 125
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"You die to A-105", "He is actually fast when u hear a sound like a train", "Is him don't hide in closet because he can check it", "Go hide in bed"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)


entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
		local entity5 = entity.Model
		if not entity5 or not firstTime then return end

		task.spawn(function()
			task.wait(0.1)
			local wardrobes = {}

			for _, v in ipairs(room:GetDescendants()) do
				if v:IsA("Model") then
					local n = string.lower(v.Name)
					if n:find("wardrobe") or n:find("closet") then
						table.insert(wardrobes, v)
					end
				end
			end

			table.sort(wardrobes, function(a, b)
				return a:GetPivot().Position.X < b:GetPivot().Position.X
			end)

			local player = game.Players.LocalPlayer
			local char = player.Character

			for _, wardrobe in ipairs(wardrobes) do
				entity:Pause()
				entity5:PivotTo(wardrobe:GetPivot() * CFrame.new(0, 0, -7))
				task.wait(0.35)

				local function isPlayerInsideWardrobe()
					if not char or not char:FindFirstChild("HumanoidRootPart") then return false end
					local boxPos = wardrobe:GetPivot().Position
					return (char.HumanoidRootPart.Position - boxPos).Magnitude < 6
				end

				local wasHiding = isPlayerInsideWardrobe()
				task.wait(1.5)

				-- Destroy Doors + Prompts
				for _, v in ipairs(wardrobe:GetDescendants()) do
					if v:IsA("ProximityPrompt") or v.Name == "HidePrompt" then v:Destroy()
					end
				end

				-- KILL + JUMPSCARE
				if isPlayerInsideWardrobe() or wasHiding then
					if char then
						task.spawn(Jumpscare) 
						task.wait(0.1)
						local hum = char:FindFirstChildOfClass("Humanoid")
						if hum then hum.Health = 0 end
					end
				end

				entity:Resume()
				task.wait(0.2)
			end
		end)

	else
		print("Entity has entered room: ".. room.Name.. " again")
			end
		end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
		local Players = game:GetService("Players")
		local TweenService = game:GetService("TweenService")

		local player = Players.LocalPlayer
		local playerGui = player:WaitForChild("PlayerGui")
		local rng = Random.new()

		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "JumpscareGui"
		screenGui.ResetOnSpawn = false
		screenGui.IgnoreGuiInset = true
		screenGui.DisplayOrder = 999999
		screenGui.Parent = playerGui

		-- Tạo background
		local background = Instance.new("Frame")
		background.Name = "Background"
		background.Size = UDim2.fromScale(1, 1)
		background.Position = UDim2.fromScale(0, 0)
		background.BackgroundColor3 = Color3.new(0, 0, 0)
		background.BorderSizePixel = 0
		background.Parent = screenGui

		-- Tạo ImageLabel
		local imageLabel = Instance.new("ImageLabel")
		imageLabel.Name = "ImageLabel"
		imageLabel.BackgroundTransparency = 1
		imageLabel.AnchorPoint = Vector2.new(0.5, 0.5)
		imageLabel.Position = UDim2.fromScale(0.5, 0.5)
		imageLabel.Size = UDim2.new(0, 0.001, 0, 0.001)
		imageLabel.Image = "rbxassetid://4817426006"
		imageLabel.Parent = background

		-- Tạo Sound
		local jumpscareSound = Instance.new("Sound")
		jumpscareSound.Name = "Jumpscare"
		jumpscareSound.SoundId = "rbxassetid://25954372"
		jumpscareSound.Volume = 10
		jumpscareSound.PlaybackSpeed = 1.95
		jumpscareSound.Parent = workspace

		local dis = Instance.new("DistortionSoundEffect")
		dis.Level = 1
		dis.Enabled = true
		dis.Parent = jumpscareSound

		local eq = Instance.new("EqualizerSoundEffect")
		eq.HighGain = 1.9
		eq.MidGain = -24.2
		eq.LowGain = 10
		eq.Enabled = true
		eq.Parent = jumpscareSound


		jumpscareSound:Play()
		
		local chanceid = {
			"8960526619",
			"6196108971",
			"6734172334",
			"7057923071",
			"4817426006"
		}

		task.spawn(function()
			while true do
				local randomId = chanceid[math.random(1, #chanceid)]
				imageLabel.Image = "rbxassetid://" .. randomId

				task.wait(0.03)
			end
		end)

		local fullTween = TweenService:Create(
			imageLabel,
			TweenInfo.new(0.8, Enum.EasingStyle.Linear, Enum.EasingDirection.Out),
			{
				Size = UDim2.new(0, 1250, 0, 1250)
			}
		)

		fullTween:Play()

		for i = 1, 80 do
			local mode = rng:NextInteger(1, 3)

			if mode == 1 then
				background.BackgroundColor3 = Color3.new(0, 0, 0)
				imageLabel.ImageColor3 = Color3.new(1, 0, 1)
			elseif mode == 2 then
				background.BackgroundColor3 = Color3.new(1, 0, 1)
				imageLabel.ImageColor3 = Color3.new(0, 0, 0)
			else
				background.BackgroundColor3 = Color3.new(0.666667, 0, 1)
				imageLabel.ImageColor3 = Color3.new(1, 0, 1)
			end

			imageLabel.Position = UDim2.new(rng:NextNumber(0.4, 0.6), 0, rng:NextNumber(0.4, 0.6), 0)
			imageLabel.Rotation = rng:NextInteger(-10, 10)
			task.wait(0.00001)
		end


		screenGui:Destroy()
		imageLabel:Destroy()
		background:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

--[[

DEVELOPER NOTE:
By overwriting 'CrucifixionOverwrite' the default crucifixion callback will be replaced with your custom callback.

entity:SetCallback("CrucifixionOverwrite", function()
    print("Custom crucifixion callback")
end)

]]--

---====== Run entity ======---

entity:Run(true)
 
        end)
    end
end)()

-- Deer God Entity
coroutine.wrap(function()
    while true do
        wait(math.random(600,200))
        pcall(function()
           ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A-200",
		Asset = "rbxassetid://17050652433",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = true,
			Duration = 5
		},
		Shatter = true,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {1.5, 20, 0.1, 1} -- Magnitude, Roughness, FadeIn, FadeOut
	},
	Movement = {
		Speed = 20,
		Delay = 5,
		Reversed = true
	},
	Rebounding = {
		Enabled = false,
		Type = "Ambush", -- "Blitz"
		Min = 1,
		Max = 1,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 10,
		Amount = 125
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding", -- "Curious"
		Hints = {"You died to A-200", "When he spawns you need to hide", "He can track you when you are not hiding!", "Remember this"},
		Cause = "A-200"
	}
})

---====== Setup Services ======---

local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")

	pcall(function()
		require(LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game).caption("Player : Something isn't right", true)
		task.wait(4)
		require(LocalPlayer.PlayerGui.MainUI.Initiator.Main_Game).caption("HIDE", true)
	end)
	task.spawn(function()
		task.wait(30)
		game.Workspace["A-200"]:Destroy()
	end)
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")

	task.spawn(function()
		while entity and entity.Model and entity.Model.Parent do
			local char = LocalPlayer.Character
			local isHiding = char and char:GetAttribute("Hiding")

			if not isHiding and char and char:FindFirstChild("HumanoidRootPart") then
				local hrp = char.HumanoidRootPart

				local model = entity.Model
				local part = model.PrimaryPart or model:FindFirstChildWhichIsA("BasePart") or model:FindFirstChild("A-200")
				part.CanCollide = false

				if part then
					local targetCFrame = hrp.CFrame
					local distance = (targetCFrame.Position - part.Position).Magnitude
					local SPEED_STUDS_PER_SECOND = 20
					local duration = math.max(0.05, distance / SPEED_STUDS_PER_SECOND)

					local tween = TweenService:Create(
						part,
						TweenInfo.new(duration, Enum.EasingStyle.Linear, Enum.EasingDirection.InOut),
						{CFrame = targetCFrame}
					)
					tween:Play()
					tween.Completed:Wait()
				else
					task.wait(0.1)
				end
			else
				task.wait(0.2)
			end
		end
	end)
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime == true then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight == true then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound == true then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")

		local sound = Instance.new("Sound")
		sound.SoundId = "rbxassetid://107732855391320"
		sound.Volume = 10
		sound.PlaybackSpeed = 1
		sound.Parent = workspace
		sound:Play()

		local screenGui = Instance.new("ScreenGui")
		screenGui.Name = "Aihoi"
		screenGui.IgnoreGuiInset = true
		screenGui.DisplayOrder = 999999
		screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

		local imageLabel = Instance.new("ImageLabel")
		imageLabel.BackgroundTransparency = 1
		imageLabel.BorderSizePixel = 0
		imageLabel.Position = UDim2.new(0, 0, 0, 0)
		imageLabel.Size = UDim2.new(1, 0, 1, 0)
		imageLabel.ScaleType = Enum.ScaleType.Stretch
		imageLabel.ImageTransparency = 0
		imageLabel.Image = "rbxassetid://16249498432"
		imageLabel.Visible = true
		imageLabel.Parent = screenGui

		task.wait(5)
		screenGui:Destroy()
		sound:Destroy()
	else
		print("Entity has damaged the player")
	end
end)

---====== Run entity ======---

entity:Run(true)

        end)
    end
end)()

-- A-225 Entity 
coroutine.wrap(function()
    while true do
        wait(math.random(615,300))
        ReplicatedStorage.GameData.LatestRoom.Changed:Wait()
        wait(0)
        pcall(function()

      ---====== Load spawner ======---

local Spawner = loadstring(game:HttpGet("https://raw.githubusercontent.com/RegularVynixu/DOORS-Entity-Spawner-V2/main/init.luau"))()

---====== Create entity ======---

local entity = Spawner:Create({
	Entity = {
		Name = "A-225",
		Asset = "rbxassetid://15800511546",
		HeightOffset = 0
	},
	Lights = {
		Flicker = {
			Enabled = false,
			Duration = 1
		},
		Shatter = false,
		Repair = false
	},
	Earthquake = {
		Enabled = false
	},
	CameraShake = {
		Enabled = true,
		Range = 100,
		Values = {5, 15, 0.1, 1}
	},
	Movement = {
		Speed = 175,
		Delay = delayTime, -- Sửa thành số nguyên
		Reversed = false
	},
	Rebounding = {
		Enabled = true,
		Type = "Ambush",
		Min = 3,
		Max = 4,
		Delay = 2
	},
	Damage = {
		Enabled = true,
		Range = 10,
		Amount = 100
	},
	Crucifixion = {
		Enabled = true,
		Range = 40,
		Resist = false,
		Break = true
	},
	Death = {
		Type = "Guiding",
		Hints = {"Death", "Hints", "Go", "Here"},
		Cause = ""
	}
})

---====== Debug entity ======---

entity:SetCallback("OnSpawned", function()
	print("Entity has spawned")
end)

entity:SetCallback("OnStartMoving", function()
	print("Entity has started moving")
end)

entity:SetCallback("OnEnterRoom", function(room: Model, firstTime: boolean)
	if firstTime then
		print("Entity has entered room: ".. room.Name.. " for the first time")
	else
		print("Entity has entered room: ".. room.Name.. " again")
	end
end)

entity:SetCallback("OnLookAt", function(lineOfSight: boolean)
	if lineOfSight then
		print("Player is looking at entity")
	else
		print("Player view is obstructed by something")
	end
end)

entity:SetCallback("OnRebounding", function(startOfRebound: boolean)
	if startOfRebound then
		print("Entity has started rebounding")
	else
		print("Entity has finished rebounding")
	end
end)

entity:SetCallback("OnDespawning", function()
	print("Entity is despawning")
end)

entity:SetCallback("OnDespawned", function()
	print("Entity has despawned")
end)

entity:SetCallback("OnDamagePlayer", function(newHealth: number)
	if newHealth <= 0 then
		print("Entity has killed the player")
	else
		print("Entity has damaged the player")
	end
end)

---====== Run entity ======---

entity:Run(true)

        end)
    end
end)()
